package com.imperium.ims.common.enums;

/**
 * Communication channel types used across notifications, email, and WhatsApp modules.
 */
public enum Channel {
    EMAIL,
    WHATSAPP,
    SMS,
    PUSH,
    IN_APP
}
